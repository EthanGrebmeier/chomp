2026-04-19 P1-T1: Added pure diffItemSnapshot helper in components/item-sheet/ with 15 colocated vitest cases covering equal state, single-field diffs, trim-equivalence, undefined/empty-string equivalence for optional fields, and multi-field diffs.
2026-04-19 P1-T2: Extracted updateGroceryItemOnly writer at features/grocery-list/instant/update-grocery-item-only.ts (grocery_items row + stores link + saved_items relink, no saved_items writes). updateGroceryListItem still contains the full logic; wrapper recomposition is P1-T4.
2026-04-19 P1-T2 (testing note): The PRD's original test plan (stub db.transact + linkStoreToItem) was implementation-detail testing — it would assert on how the writer talks to Instant rather than any observable outcome. Dropped the vitest file and updated the plan's test plan entries for P1-T2 and P1-T3 to defer correctness validation to the P1-T4 wrapper smoke test and later end-to-end flows. PRD "Testing Decisions" section still mentions these stubs; flagged here for the user to reconcile.
2026-04-19 P1-T3: Extracted syncSavedItemFromGroceryItem writer at features/grocery-list/instant/sync-saved-item-from-grocery-item.ts (saved_items row + saved_items↔stores reconcile + owner gate + local-saved-item upsert). updateGroceryListItem still contains the full logic; wrapper recomposition is P1-T4. Note: wrapper will need to pass currentSavedItemOwnerId = user.id when a freshly-picked selectedSavedItemId is being written, to preserve the old "assume owner on pick" shortcut until P5 carries real ownerId on matches.
2026-04-19 P1-T4: Recomposed updateGroceryListItem as a thin wrapper over updateGroceryItemOnly + syncSavedItemFromGroceryItem. Preserved the legacy "assume owner on pick" shortcut by resolving db.getAuth() and passing user.id as currentSavedItemOwnerId when selectedSavedItemId is the next linked saved item. No call-site changes (edit-item-sheet.tsx and check-list-item.ts still import the wrapper unchanged). Validated with pnpm test (62 passing), pnpm lint (0 errors), pnpm tsc --noEmit (clean).
2026-04-19 P2-T1: Surfaced ownerId through the unified-saved-items layer. Added `user: {}` projection to the cloud saved_items query, set UnifiedSavedItem.ownerId = item.user?.id for cloud rows (undefined for local), added optional MatchingItem.ownerId, and populated it only for cloud matches in useMatchingItems. No consumer wiring yet (deferred to P5). Validated with pnpm tsc --noEmit (clean) and pnpm lint (no new warnings).
2026-04-19 P3-T1: Added components/item-sheet/edit-item/use-live-item-sync.ts. Hook captures an ItemSnapshot on present, fires immediate updateGroceryItemOnly writes on category/quantity/unit/storeId changes (guarded by snapshot diff + non-empty-name check), debounces name/notes writes at 300ms via useDebounceCallback, and exposes flushAndSyncOnClose that flushes the pending debounce and (for P3 scope) routes the close-time sync through the existing updateGroceryListItem wrapper. Composed the hook into EditItemProvider via a sibling EditItemLiveSync component inside ItemSheetProvider, published its imperative handle on the internal context ref, and now captureSnapshot(item) runs during present() and clearSnapshot() runs alongside reset() in the sheet's onStartClose. The Update Item button / toast / dismiss wiring is intentionally untouched (deferred to P3-T2). Validated with pnpm tsc --noEmit (clean), pnpm lint (no new warnings), pnpm test (62 passing).
2026-04-19 P3-T2: Removed the Update Item button, its footer container, the sonner toast, and the updateGroceryListItem+unlinkRecipeFromItem onSubmit body from components/item-sheet/edit-item/edit-item-sheet.tsx. Replaced onSubmit with a no-op to satisfy ItemSheetProvider's shared contract (return-key in the name input still lands here and will be swapped for a blur in P3-T3). Split the sheet lifecycle: onStartClose now calls liveSyncRef.current?.flushAndSyncOnClose() (fires before dismiss completes, so form state is still current), onDismiss calls reset() + liveSyncRef.current?.clearSnapshot() (fires after dismiss completes). Fixed BottomSheet's event wiring to make this split possible: onStartClose is now mapped to TrueSheet's onWillDismiss (was incorrectly mapped to onDidDismiss), and a new onDismiss prop is mapped to onDidDismiss. Migrated the three existing callers that relied on the old "onStartClose == did-dismiss" semantics — components/item-sheet/edit-unit-sheet.tsx, components/item-sheet/add-item/add-item-sheet.tsx, components/calendar-sheet.tsx — to use the new onDismiss prop so their post-close state resets keep their current behavior. Add Item sheet button, isValid gating, and success toast left untouched per the PRD's scope fence. Validated with pnpm tsc --noEmit (clean), pnpm lint (no new warnings), pnpm test (62 passing).
2026-04-19 P3-T3: Routed the name input's onSubmitEditing through a mode-aware handler in components/item-sheet/item-form.tsx. When useItemSheet().mode === 'update', return key calls itemInputRef.current?.blur() so the soft keyboard dismisses without firing the sheet's (now no-op) onSubmit; when mode === 'add', onSubmit is wired through unchanged so the Add sheet keeps its submit-on-return behavior. No writes are triggered as a direct consequence of the return key — the live 300ms debounce in useLiveItemSync still owns name persistence. Validated with pnpm tsc --noEmit (clean) and pnpm lint (no new warnings; the 3 pre-existing warnings in unrelated files remain unchanged).

2026-04-19 [P1-T1]: Added features/recipes/components/edit-ingredient/diff-ingredient-snapshot.ts — thin wrapper around the shared diff-item-snapshot helper so the edit-ingredient subfolder is self-contained. Re-exports IngredientSnapshot/IngredientSnapshotDiff aliases and a diffIngredientSnapshot({ snapshot, current }) function. pnpm tsc --noEmit passes; pnpm lint shows only pre-existing warnings.
2026-04-19 [P1-T2]: Added features/recipes/components/edit-ingredient/use-live-ingredient-sync.ts — useLiveIngredientSync hook owning snapshotRef + currentStoreIdRef + stateRef, with 300ms trailing debounce via useDebounceCallback for name/notes, immediate commits for category/quantity/unit/storeId, isValid gating, and an imperative handle (captureSnapshot / flush / clearSnapshot / onPickMatch stub) published via handleRef. Successful writes rebase snapshotRef to the committed values and roll currentStoreIdRef forward when storeId was in the diff. onPickMatch is a stub in this ticket; real impl lands in P2-T1. pnpm tsc --noEmit passes; pnpm lint shows only pre-existing warnings.
2026-04-19 [P1-T3]: Added features/recipes/components/edit-ingredient/edit-ingredient-live-sync.tsx — thin null-rendering consumer that runs useLiveIngredientSync inside ItemSheetProvider. Takes { selectedIngredientId, currentStoreId, liveSyncRef } as props (no internal context, since AddIngredientProvider is a unified add/edit provider) and re-exports LiveIngredientSyncHandle for the host provider. Wiring into AddIngredientProvider lands in P1-T4. pnpm tsc --noEmit passes; pnpm lint shows only pre-existing warnings.
2026-04-19 [P1-T4]: Wired EditIngredientLiveSync into AddIngredientProvider in features/recipes/components/add-ingredient-sheet.tsx. Provider now owns a liveSyncRef: Ref<LiveIngredientSyncHandle | null>, renders <EditIngredientLiveSync /> inside ItemSheetProvider only when isEditing, and publishes { sheetRef, liveSyncRef, isEditing } through the internal context so AddIngredientContents can branch. Edit-mode: footer button is omitted, onSubmit is a no-op (live writes handle persistence; success toast + manual sheetRef.dismiss() removed from the edit branch), present() calls liveSyncRef.current?.captureSnapshot(ingredient) after setFromItemRef and before sheetRef.present(), BottomSheet onStartClose flushes the pending debounce then reset()s, and a new BottomSheet onDismiss reset()s and clearSnapshot()s. Add mode: footer button, toast, and continuous-entry behavior are unchanged — onSubmit only runs addRecipeIngredient + toast in that branch, onStartClose is just reset(), and onDismiss is a no-op. Autocomplete-pick wiring (onPickMatch prop into ItemSheetProvider) is still P2-T2. pnpm tsc --noEmit passes; pnpm lint shows only pre-existing warnings.
2026-04-19 [P2-T1]: Replaced the P1 onPickMatch stub in features/recipes/components/edit-ingredient/use-live-ingredient-sync.ts with the real implementation. The handler now short-circuits on missing snapshot / selectedIngredientId / isValid, cancels debouncedCommit to drop any stale pre-pick keystroke, then invokes updateRecipeIngredient with the picked name/category/notes/storeId (unit from match with fallback to form unit; match.unit is always present today) while preserving the form's current quantity. currentStoreIdRef is passed through so linkStoreToIngredient reconciles against the real linked store. After the write is queued, snapshotRef rebases to the picked values so subsequent edits diff against the post-pick state, and currentStoreIdRef rolls forward to match.storeId when the pick carries one. No saved_items relink is performed. Wiring the onPickMatch prop into ItemSheetProvider in add-ingredient-sheet.tsx is still P2-T2. pnpm tsc --noEmit passes; pnpm lint shows only pre-existing warnings.
